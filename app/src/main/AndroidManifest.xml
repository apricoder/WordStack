<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.olebokolo.wordstack">

    <application
        android:name=".core.app.WordStack"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="DATABASE"
            android:value="wordstack.db" />
        <meta-data
            android:name="VERSION"
            android:value="3" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.olebokolo.wordstack" />

        <activity android:name=".presentation.activities.MainMenuActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.activities.LanguagesActivity" />
        <activity android:name=".presentation.activities.GreetingActivity" />
        <activity android:name=".presentation.activities.SettingsActivity" />
        <activity android:name=".presentation.activities.StackListActivity"></activity>
    </application>

</manifest>